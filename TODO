The following task should be done in order(or maybe not) before
the first version released.

1. exec			(0.7) 
2. robuust shell	(0.8)
3. sys call		(0.9)

== And the final 1.0 ==
4. port some coreutils from GNU like cp, cat and so on into thunix.
